describe('Sample Test Suite', () => {
  it('should correctly sum two numbers', () => {
    expect(1 + 2).toBe(3)
  })

  it('should correctly handle basic TypeScript features', () => {
    const greeting: string = 'Hello, FinanceFlow!'
    expect(greeting).toBe('Hello, FinanceFlow!')
  })
})
